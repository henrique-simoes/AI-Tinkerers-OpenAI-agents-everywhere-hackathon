# <idea name>

**Status:** draft | `viable` | `viable-if <condition>` | `rejected`

## Demo sentence
> One sentence a judge hears over the 2-minute video. Two sentences means the scope is too big.

## The untapped context
Which tool / channel / device / environment does this agent live inside, and why does *that*
context give it leverage it would not have in a chat window?

## Who it's for
A specific person with a specific recurring problem. Not "users".

## What the agent actually does
The agency, not the plumbing. Where does it **decide, act, or interrupt** rather than fetch
and display?

## Scope cut for 4h15m
| | |
|---|---|
| **Ships for real** | |
| **Stubbed / seeded for the demo** | |
| **Explicitly out of scope** | |

## Sponsor hooks
| Sponsor | Load-bearing? | How |
|---|---|---|
| | | |

Named prizes: **Best Use of CopilotKit**, **Best Use of Ambiguous AI**.
Load-bearing means the project does not work without it.

## Kill risks
Ranked, most likely first.
1.
2.
3.

## Checks
- [ ] Works as a **public GitHub repo** — no ToS violation, no raw credential handling
- [ ] Buildable in **4h15m** including the five deliverables
- [ ] Demoable in **90 seconds** on congested venue Wi-Fi
- [ ] Fits **"agent in an untapped context"**, not "chatbot with a wrapper"
- [ ] Has an **offline / seeded fallback** for the video
- [ ] No slow gate (app store, OAuth review, manual approval, device provisioning)

## Verdict
`viable` / `viable-if <condition>` / `rejected` — plus one line of reasoning.

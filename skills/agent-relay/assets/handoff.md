# Handoff: <what this covers>

**From:** <handle> · **At:** <YYYY-MM-DDTHH:MM±HH:MM> · **Claim:** `<path glob or item>`
**Reason:** <finished | blocked | stopping | out of time>

> Write this assuming **you will not be the one to continue**, and that whoever does has
> none of your context and is running a different model. No shorthand, no "as discussed".

## State
<What is true in the repo right now. What works, what does not, what is half-done. Be
specific: file paths and line numbers beat descriptions.>

## Where it lives
- `<path>` — <what is in it>

## Next step
<The single next action, concretely. Not "continue the UI" — "wire the approve button in
web/src/desk/Card.tsx:52 to POST /api/desk/approve; the stub already returns ok:true".>

## What I would not do
<Dead ends you already tried, so the next agent does not repeat them. This is often the
most valuable section.>

## Contracts affected
<Any contract this touches, and whether it is still accurate.>

## Verification
<How to check it works. The exact command, and what passing looks like.>

```bash
<command>
```
